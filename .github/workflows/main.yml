name: Build iPhone CAD App (Unsigned IPA)

on:
  workflow_dispatch: # গিটহাব থেকে ম্যানুয়ালি রান করার জন্য

jobs:
  build:
    runs-on: macos-14
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Create Project Directory Structure
        run: |
          mkdir -p AutoCADMobile

      - name: Create Info.plist
        run: |
          cat << 'EOF' > AutoCADMobile/Info.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleIdentifier</key>
              <string>com.sifat.AutoCADMobile</string>
              <key>CFBundleName</key>
              <string>MiniCAD</string>
              <key>CFBundleShortVersionString</key>
              <string>1.0</string>
              <key>CFBundleVersion</key>
              <string>1</string>
              <key>LSRequiresIPhoneOS</key>
              <true/>
              <key>UIApplicationSceneManifest</key>
              <dict>
                  <key>UIApplicationSupportsMultipleScenes</key>
                  <false/>
                  <key>UISceneConfigurations</key>
                  <dict>
                      <key>UIWindowSceneSessionRoleApplication</key>
                      <array>
                          <dict>
                              <key>UISceneConfigurationName</key>
                              <string>Default Configuration</string>
                              <key>UISceneDelegateClassName</key>
                              <string>AutoCADMobile.SceneDelegate</string>
                          </dict>
                      </array>
                  </dict>
              </dict>
              <key>UILaunchStoryboardName</key>
              <string>LaunchScreen</string>
              <key>UIRequiredDeviceCapabilities</key>
              <array>
                  <string>arm64</string>
              </array>
              <key>UISupportedInterfaceOrientations</key>
              <array>
                  <string>UIInterfaceOrientationPortrait</string>
                  <string>UIInterfaceOrientationLandscapeLeft</string>
                  <string>UIInterfaceOrientationLandscapeRight</string>
              </array>
          </dict>
          </plist>
          EOF

      - name: Create Lifecycle & Core Files (AppDelegate & SceneDelegate)
        run: |
          cat << 'EOF' > AutoCADMobile/AppDelegate.swift
          import UIKit

          @main
          class AppDelegate: UIResponder, UIApplicationDelegate {
              func application(_ application: UIApplication, didFinishLaunchingWithOptions launchOptions: [UIApplication.LaunchOptionsKey: Any]?) -> Bool {
                  return true
              }
              func application(_ application: UIApplication, configurationForConnecting connectingSceneSession: UISceneSession, options: UIScene.ConnectionOptions) -> UISceneConfiguration {
                  return UISceneConfiguration(name: "Default Configuration", sessionRole: connectingSceneSession.role)
              }
          }
          EOF

          cat << 'EOF' > AutoCADMobile/SceneDelegate.swift
          import UIKit

          class SceneDelegate: UIResponder, UIWindowSceneDelegate {
              var window: UIWindow?
              func scene(_ scene: UIScene, willConnectTo session: UISceneSession, options connectionOptions: UIScene.ConnectionOptions) {
                  guard let windowScene = (scene as? UIWindowScene) else { return }
                  window = UIWindow(windowScene: windowScene)
                  let mainVC = ViewController()
                  window?.rootViewController = mainVC
                  window?.makeKeyAndVisible()
              }
          }
          EOF

      - name: Create CAD Logic Engine (CADEngine.swift)
        run: |
          cat << 'EOF' > AutoCADMobile/CADEngine.swift
          import UIKit

          // ক্যাড ড্রয়িং এলিমেন্ট টাইপ
          enum CADShapeType {
              case line, circle, rectangle, freehand
          }

          // প্রতিটি ড্রয়িং অবজেক্টের স্ট্রাকচার
          struct CADElement {
              var type: CADShapeType
              var points: [CGPoint]
              var color: UIColor
              var strokeWidth: CGFloat
          }

          // ফাইল হ্যান্ডলিং (DWG/DXF পার্সার বেসিক আর্কিটেকচার)
          class CADEngine {
              var elements: [CADElement] = []
              
              // মক ডিডব্লিউজি রিডার: ফাইল ইমপোর্ট লজিক
              func importDXF_DWG(from url: URL) -> Bool {
                  // রিয়েল প্রজেক্টে এখানে libopencad বা dxflib C++ ব্রিজ কল হবে।
                  // প্রাথমিক টেস্টিং এর জন্য কিছু ডেমো ভেক্টর লাইন জেনারেট করা হচ্ছে।
                  elements.removeAll()
                  elements.append(CADElement(type: .rectangle, points: [CGPoint(x: 50, y: 150), CGPoint(x: 300, y: 400)], color: .cyan, strokeWidth: 2.0))
                  elements.append(CADElement(type: .line, points: [CGPoint(x: 50, y: 150), CGPoint(x: 300, y: 400)], color: .red, strokeWidth: 1.5))
                  elements.append(CADElement(type: .circle, points: [CGPoint(x: 175, y: 275), CGPoint(x: 275, y: 275)], color: .green, strokeWidth: 2.0))
                  return true
              }
          }
          EOF

      - name: Create Custom UI Interactive Canvas (CanvasView.swift)
        run: |
          cat << 'EOF' > AutoCADMobile/CanvasView.swift
          import UIKit

          class CanvasView: UIView {
              var engine: CADEngine?
              var currentShapeType: CADShapeType = .line
              var currentColor: UIColor = .white
              var currentStrokeWidth: CGFloat = 2.0
              
              private var currentPoints: [CGPoint] = []

              override init(frame: CGRect) {
                  super.init(frame: frame)
                  self.backgroundColor = UIColor(white: 0.1, alpha: 1.0) // অটোক্যাড ব্ল্যাক থিম ব্যাকগ্রাউন্ড
              }

              required init?(coder: NSCoder) {
                  fatalError("init(coder:) has not been implemented")
              }

              override func draw(_ rect: CGRect) {
                  guard let context = UIGraphicsGetCurrentContext() else { return }
                  context.setLineCap(.round)
                  context.setLineJoin(.round)

                  // ১. গ্রিড লাইন ড্র করা (অটোক্যাড লেআউট ফিল)
                  drawGrid(context, rect: rect)

                  // ২. ইঞ্জিনে থাকা সমস্ত সেভড এলিমেন্ট ড্র করা
                  if let elements = engine?.elements {
                      for element in elements {
                          context.setStrokeColor(element.color.cgColor)
                          context.setLineWidth(element.strokeWidth)
                          drawShape(context, type: element.type, points: element.points)
                      }
                  }

                  // ৩. ইউজার বর্তমানে যা ড্র করছে (লাইভ প্রিভিউ)
                  if !currentPoints.isEmpty {
                      context.setStrokeColor(currentColor.cgColor)
                      context.setLineWidth(currentStrokeWidth)
                      drawShape(context, type: currentShapeType, points: currentPoints)
                  }
              }

              private func drawGrid(_ context: CGContext, rect: CGRect) {
                  context.setStrokeColor(UIColor(white: 0.2, alpha: 0.5).cgColor)
                  context.setLineWidth(0.5)
                  let gridSize: CGFloat = 40.0
                  
                  // ভার্টিক্যাল গ্রিড
                  for x in stride(from: 0, to: rect.width, by: gridSize) {
                      context.move(to: CGPoint(x: x, y: 0))
                      context.addLine(to: CGPoint(x: x, y: rect.height))
                  }
                  // হরাইজন্টাল গ্রিড
                  for y in stride(from: 0, to: rect.height, by: gridSize) {
                      context.move(to: CGPoint(x: 0, y: y))
                      context.addLine(to: CGPoint(x: rect.width, y: y))
                  }
                  context.strokePath()
              }

              private func drawShape(_ context: CGContext, type: CADShapeType, points: [CGPoint]) {
                  guard !points.isEmpty else { return }
                  switch type {
                  case .line, .freehand:
                      context.move(to: points[0])
                      for i in 1..<points.count {
                          context.addLine(to: points[i])
                      }
                  case .circle:
                      if points.count >= 2 {
                          let center = points[0]
                          let radius = sqrt(pow(points[1].x - center.x, 2) + pow(points[1].y - center.y, 2))
                          context.addArc(center: center, radius: radius, startAngle: 0, endAngle: .pi * 2, clockwise: true)
                      }
                  case .rectangle:
                      if points.count >= 2 {
                          let origin = points[0]
                          let size = CGSize(width: points[1].x - origin.x, height: points[1].y - origin.y)
                          context.addRect(CGRect(origin: origin, size: size))
                      }
                  }
                  context.strokePath()
              }

              // টাচ ইন্টারঅ্যাকশন কন্ট্রোল (Drawing & Editing Tools)
              override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
                  guard let touch = touches.first else { return }
                  let location = touch.location(in: self)
                  currentPoints = [location]
                  setNeedsDisplay()
              }

              override func touchesMoved(_ touches: Set<UITouch>, with event: UIEvent?) {
                  guard let touch = touches.first else { return }
                  let location = touch.location(in: self)
                  
                  if currentShapeType == .freehand {
                      currentPoints.append(location)
                  } else {
                      if currentPoints.count > 1 {
                          currentPoints[1] = location
                      } else {
                          currentPoints.append(location)
                      }
                  }
                  setNeedsDisplay()
              }

              override func touchesEnded(_ touches: Set<UITouch>, with event: UIEvent?) {
                  if currentPoints.count > 0 {
                      let newElement = CADElement(type: currentShapeType, points: currentPoints, color: currentColor, strokeWidth: currentStrokeWidth)
                      engine?.elements.append(newElement)
                  }
                  currentPoints.removeAll()
                  setNeedsDisplay()
              }
          }
          EOF

      - name: Create UI Controller & Layout Panel (ViewController.swift)
        run: |
          cat << 'EOF' > AutoCADMobile/ViewController.swift
          import UIKit

          class ViewController: UIViewController, UIDocumentPickerDelegate {
              let engine = CADEngine()
              var canvasView: CanvasView!
              
              // কন্ট্রোল প্যানেল UI এলিমেন্ট
              let topBar = UIView()
              let bottomTools = UIScrollView()

              override func viewDidLoad() {
                  super.viewDidLoad()
                  view.backgroundColor = .black
                  setupCanvas()
                  setupTopBar()
                  setupBottomTools()
              }

              func setupCanvas() {
                  canvasView = CanvasView(frame: view.bounds)
                  canvasView.engine = engine
                  canvasView.autoresizingMask = [.flexibleWidth, .flexibleHeight]
                  view.addSubview(canvasView)
              }

              func setupTopBar() {
                  topBar.frame = CGRect(x: 0, y: 0, width: view.frame.width, height: 90)
                  topBar.backgroundColor = UIColor(white: 0.15, alpha: 0.9)
                  topBar.autoresizingMask = [.flexibleWidth]
                  view.addSubview(topBar)

                  // টাইটেল লেবেল
                  let titleLabel = UILabel(frame: CGRect(x: 20, y: 45, width: 150, height: 35))
                  titleLabel.text = "Premium CAD v1.0"
                  titleLabel.textColor = .white
                  titleLabel.font = UIFont.boldSystemFont(ofSize: 18)
                  topBar.addSubview(titleLabel)

                  // ফাইল ইমপোর্ট বাটন (DWG/DXF এর জন্য)
                  let importBtn = UIButton(frame: CGRect(x: view.frame.width - 120, y: 45, width: 100, height: 35))
                  importBtn.setTitle("Open DWG", for: .normal)
                  importBtn.backgroundColor = .systemBlue
                  importBtn.layer.cornerRadius = 8
                  importBtn.titleLabel?.font = UIFont.systemFont(ofSize: 14, weight: .medium)
                  importBtn.autoresizingMask = [.flexibleLeftMargin]
                  importBtn.addTarget(self, action: #selector(openFilePicker), for: .touchUpInside)
                  topBar.addSubview(importBtn)
              }

              func setupBottomTools() {
                  bottomTools.frame = CGRect(x: 0, y: view.frame.height - 80, width: view.frame.width, height: 80)
                  bottomTools.backgroundColor = UIColor(white: 0.15, alpha: 0.9)
                  bottomTools.autoresizingMask = [.flexibleWidth, .flexibleTopMargin]
                  view.addSubview(bottomTools)

                  // ড্রয়িং টুলস বাটন এরে
                  let tools = ["Line", "Circle", "Rect", "Free", "Clear"]
                  var currentX: CGFloat = 15
                  
                  for tool in tools {
                      let btn = UIButton(frame: CGRect(x: currentX, y: 15, width: 70, height: 40))
                      btn.setTitle(tool, for: .normal)
                      btn.backgroundColor = UIColor(white: 0.25, alpha: 1.0)
                      btn.layer.cornerRadius = 6
                      btn.titleLabel?.font = UIFont.systemFont(ofSize: 13, weight: .bold)
                      btn.addTarget(self, action: #selector(toolTapped(_:)), for: .touchUpInside)
                      bottomTools.addSubview(btn)
                      currentX += 80
                  }
                  bottomTools.contentSize = CGSize(width: currentX, height: 80)
              }

              @objc func toolTapped(_ sender: UIButton) {
                  guard let title = sender.currentTitle else { return }
                  
                  // বাটন ফিডব্যাক হাইলাইট
                  for case let btn as UIButton in bottomTools.subviews {
                      btn.backgroundColor = UIColor(white: 0.25, alpha: 1.0)
                  }
                  sender.backgroundColor = .systemGreen

                  switch title {
                  case "Line": canvasView.currentShapeType = .line
                  case "Circle": canvasView.currentShapeType = .circle
                  case "Rect": canvasView.currentShapeType = .rectangle
                  case "Free": canvasView.currentShapeType = .freehand
                  case "Clear":
                      engine.elements.removeAll()
                      canvasView.setNeedsDisplay()
                  default: break
                  }
              }

              @objc func openFilePicker() {
                  let picker = UIDocumentPickerViewController(forOpeningContentTypes: [.data], asCopy: true)
                  picker.delegate = self
                  present(picker, animated: true)
              }

              func documentPicker(_ controller: UIDocumentPickerViewController, didPickDocumentsAt urls: [URL]) {
                  guard let url = urls.first else { return }
                  if engine.importDXF_DWG(from: url) {
                      canvasView.setNeedsDisplay() // ফাইল ডেটা লোড শেষে ক্যানভাস রিফ্রেশ
                  }
              }
          }
          EOF

      - name: Compile and Build Unsigned IPA via xcodebuild
        run: |
          # ক্যাটালগ এবং প্রোফাইল ছাড়া সুইফট ফাইলগুলো কম্পাইল করে সাইনড বাইপাস মেক করা
          cd AutoCADMobile
          sdk_path=$(xcrun --sdk iphoneos --show-sdk-path)
          
          swiftc -sdk "$sdk_path" -target arm64-apple-ios15.0 \
            -o MiniCAD \
            AppDelegate.swift SceneDelegate.swift CADEngine.swift CanvasView.swift ViewController.swift
          
          # আইওএস রিকোয়ারমেন্ট স্ট্রাকচার জেনারেট (Payload মেকিং)
          mkdir -p Payload/MiniCAD.app
          mv MiniCAD Payload/MiniCAD.app/
          cp Info.plist Payload/MiniCAD.app/
          
          # জিপ ফাইল জেনারেট করে .ipa ফরম্যাটে কনভার্ট
          zip -r ../MiniCAD_Unsigned.ipa Payload
          
      - name: Upload Unsigned IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AutoCAD-Mobile-Unsigned-IPA
          path: MiniCAD_Unsigned.ipa
